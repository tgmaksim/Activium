<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Разрешения приложения: интернет, уведомления -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Activium.Splash">

        <!-- Загрузчик всего приложения -->
        <activity
            android:name=".LauncherActivity"
            android:exported="true"
            android:theme="@style/Theme.Activium.Splash">

            <!-- Определение главного Activity -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Открытие ссылок в приложении -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- tgm://activium/open — открывает приложение -->
                <data
                    android:scheme="tgm"
                    android:host="activium"
                    android:path="/open" />

            </intent-filter>

        </activity>

        <!-- Другие Activity приложения -->
        <activity android:name=".ui.main.MainActivity" android:exported="false" android:launchMode="singleTask" />
        <activity android:name=".ui.LoginActivity" android:exported="false" android:launchMode="singleTask" />
        <activity android:name=".ui.webview.WebSchoolPostActivity" android:exported="false" />

        <!-- Service для уведомлений -->
        <service android:name=".utilities.MessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <receiver android:name=".utilities.NotificationActionReceiver" android:exported="false" />

        <!-- Регистрация через Firebase Installation ID -->
        <meta-data
            android:name="firebase_messaging_installation_id_enabled"
            android:value="true" />

        <!-- Не запускать автоматически Firebase  -->
        <provider
            android:name="com.google.firebase.provider.FirebaseInitProvider"
            android:authorities="${applicationId}.firebaseinitprovider"
            android:exported="false"
            tools:node="remove" />

    </application>

</manifest>